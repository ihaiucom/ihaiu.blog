using System;
using System.Collections.Generic;
using System.Text;

namespace ZF
{
    public enum DefaultUiElement
    {
        // 默认UI元素：无效
        INVALID = -1,
        // 默认UI元素：时间和天气
        TOP_TIMEOFDAY = 0,
        // 默认UI元素：英雄
        TOP_HEROES = 1,
        // 默认UI元素：得分面板
        FLYOUT_SCOREBOARD = 2,
        // 默认UI元素：动作面板
        ACTION_PANEL = 3,
        // 默认UI元素：小地图
        ACTION_MINIMAP = 4,
        // 默认UI元素：背包面板
        INVENTORY_PANEL = 5,
        // 默认UI元素：商店
        INVENTORY_SHOP = 6,
        // 默认UI元素：物品
        INVENTORY_ITEMS = 7,
        // 默认UI元素：快捷购买
        INVENTORY_QUICKBUY = 8,
        // 默认UI元素：信使
        INVENTORY_COURIER = 9,
        // 默认UI元素：保护
        INVENTORY_PROTECT = 10,
        // 默认UI元素：金钱
        INVENTORY_GOLD = 11,
        // 默认UI元素：推荐物品
        SHOP_SUGGESTEDITEMS = 12,
        // 默认UI元素：常用物品
        SHOP_COMMONITEMS = 13,
        // 默认UI元素：英雄选择队伍
        HERO_SELECTION_TEAMS = 14,
        // 默认UI元素：英雄选择游戏名称
        HERO_SELECTION_GAME_NAME = 15,
        // 默认UI元素：英雄选择时钟
        HERO_SELECTION_CLOCK = 16,
        // 默认UI元素：英雄选择标题
        HERO_SELECTION_HEADER = 17,
        // 默认UI元素：顶部菜单按钮
        TOP_MENU_BUTTONS = 18,
        // 默认UI元素：顶部栏背景
        TOP_BAR_BACKGROUND = 19,
        // 默认UI元素：辐射队伍顶部栏
        TOP_BAR_RADIANT_TEAM = 20,
        // 默认UI元素：天辉队伍顶部栏
        TOP_BAR_DIRE_TEAM = 21,
        // 默认UI元素：得分顶部栏
        TOP_BAR_SCORE = 22,
        // 默认UI元素：结束游戏
        ENDGAME = 23,
        // 默认UI元素：结束游戏聊天
        ENDGAME_CHAT = 24,
        // 默认UI元素：快速统计
        QUICK_STATS = 25,
        // 默认UI元素：前期策略UI
        PREGAME_STRATEGYUI = 26,
        // 默认UI元素：击杀镜头
        KILLCAM = 27,
        // 默认UI元素：战斗回顾
        FIGHT_RECAP = 28,
        // 默认UI元素：顶部栏
        TOP_BAR = 29,
        // 默认UI元素：自定义UI在HUD元素后面
        CUSTOMUI_BEHIND_HUD_ELEMENTS = 30,
        // 默认UI元素：阿哈利姆状态
        AGHANIMS_STATUS = 31,
    }
}

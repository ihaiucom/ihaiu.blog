

import ExcelConfigReader from "../ExcelConfigReader";
import GashaponConfig from "../ConfigExtends/GashaponConfig";
import GashaponType = proto.GashaponType;

export default class GashaponConfigReader extends ExcelConfigReader<GashaponConfig>
{
    public getRewardIdByGashapon(type: GashaponType)
    {

    }
}